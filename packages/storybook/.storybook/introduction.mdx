import { Meta, Story, Source } from "@storybook/addon-docs";

<Meta title="Introduction" />

<br />

# Introduction 👋🏼

基于 [HeroUI](https://github.com/heroui-inc/heroui) V2 + Tailwind CSS 3 扩展的适用于手机端的组件库。对过时、不支持现代浏览器特性的手机端浏览器更友好。

使用 [Storybook](https://storybook.js.org/) 管理扩展组件，使用 [Shadcn CLI](https://github.com/shadcn/cli) 注册、安装组件示例。

<br />

<div className="sb-container">
  <div className='sb-section-title'>
    # 配置安装 Heroui V2.7.11

    参考 [Heroui Initialization](https://canary.heroui.com/docs/guide/installation#initialization-and-starting-the-app)。
  </div>
  <Source language="bash" dark={true} code={
`
pnpm add @heroui/react@2.7.11 framer-motion
`
    }
  />

<p style={{ marginBottom: "-20px" }}>如果你使用PNPM，需要在项目根目录创建 `.npmrc` 文件，并添加以下内容：</p>
  <Source
    language="bash"
    dark={true}
    code={
`
public-hoist-pattern[]=*@heroui/*
public-hoist-pattern[]=*@heroui-mobile/*
`}
  />
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # 配置项目以支持 shadcn CLI

    配置您的项目，以便可以通过 `shadcn CLI` 添加组件示例，你的项目至少需要包含以下内容：
  </div>

  <p style={{ marginBottom: "-20px" }}>`components.json`</p>
  <Source
    language="json"
    dark={true}
    code={
`{
  "$schema": "https://ui.shadcn.com/schema.json",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "css": "app/styles/global.css",
    "baseColor": "",
    "cssVariables": true,
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide",
  "registries": {
    "@heroui-mobile": "https://main--69549dc20eb3fc63d6bc3d0e.chromatic.com/r/{name}.json"
  }
}`
    }
  />

  <p style={{ marginBottom: "-20px" }}>`package.json`</p>
  <Source
    language="json"
    dark={true}
    code={
`{
  "devDependencies": {
    "@types/node": "20.19.9",
    "@types/react": "18.3.1",
    "@types/react-dom": "18.3.1"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-router-dom": "7.11.0",
    "@heroui/react": "2.7.11",
    "@heroui-mobile/react": "latest",
  }
}`
    }
  />

  <p style={{ marginBottom: '-20px' }}>`tsconfig.json`</p>
  <Source language="json" dark={true} code={
`{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "strict": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "paths": {
      "@/*": ["./app/*"]
    }
  }
}`
  } />
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # 初始化 & 安装组件

    使用 `shadcn CLI` 的 `search` 命令来搜索组件示例，`add` 命令来添加组件示例。
  </div>
  <Source language="bash" dark={true} code={
`
npx shadcn@latest add @heroui-mobile/example-fruits
npx shadcn@latest search @heroui-mobile
`
    }
  />
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # MCP 服务器

    为注册表启用 MCP 支持，这样你就可以在 `LLM` 中使用提示词来使用组件示例了。
  </div>

  <p style={{ marginBottom: "-20px" }}>`.vscode/mcp.json` or `.cursor/mcp.json`</p>
  <Source
    language="json"
    dark={true}
    code={
`{
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}`
    }
  />

  <p style={{ marginBottom: '-20px' }}>然后，你可以在 `LLM` 中使用提示词来使用组件</p>
  <Source language="bash" dark={true} code={
`
Show me the components in the @heroui-mobile registry
Create a landing page using items from the @heroui-mobile registry
`
  } />
</div>

<style>
{`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section a:not(h1 a, h2 a, h3 a) {
    font-size: 14px;
  }

  @media screen and (max-width: 600px) {
    .sb-section {
      flex-direction: column;
    }
  }
`}
</style>
